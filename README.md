# AI-assistant-Console-granite-IBM
Input: The CLI prompts you for a markdown file URL (e.g., a raw GitHub .md file). - Processing: The assistant downloads the file, splits it into chunks, and creates embeddings for fast search. - Chat: Ask questions in the terminal. The assistant finds the most relevant content and generates an answer using a local LLM.
